import React from "react";
import {View, Text} from 'react-native';
import styles from "./styles";

const model = function(image) {
   console.warn(image);
   
   return (
      <View></View>
   );
}

export default model;
